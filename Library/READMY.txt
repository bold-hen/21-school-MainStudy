Это библиотека которую я буду использовать в проектах для основного обучения на языке С и корневой Makefile для компиляции проектов

Далее идет список функций которые я хочу дописать в библиотеку или переработать:

1) парсинг строки регулярными выражениями
2) переработать все существующие функции если это необходимо

Далее идет список уже реализованных функций с их прототипами и описанием:

